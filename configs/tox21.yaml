# configs/tox21.yaml
# Tox21: 12 binary classification sub-tasks, ~7% positive rate, ~7.8k molecules
model:
  hidden_dim: 128
  n_layers: 4
  num_tasks: 12

training:
  batch: 64
  lr: 0.001
  epochs: 200
  patience: 30
